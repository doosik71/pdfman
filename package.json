{
  "name": "pdfman",
  "version": "1.0.0",
  "description": "PDFMan은 PDF 문서를 관리하는 Node.js 기반 웹 앱이다. 프론트엔드는 React 프레임워크를 사용한다. Electron을 이용하여 데스크톱 앱으로 패키징할 수 있다. Gemini를 이용해 PDF 문서를 요약하고 요약 정보를 파일에 저장할 수 있다.",
  "main": "main.js",
  "scripts": {
    "start": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "electron-dev": "concurrently \"npm run start\" \"electron .\"",
    "build-electron": "npm run build --prefix client && electron-builder"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "axios": "^1.11.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "electron": "^29.1.0",
    "electron-builder": "^24.13.3",
    "express": "^5.1.0",
    "multer": "^2.0.2",
    "nodemon": "^3.1.10",
    "pdf-parse": "^1.1.1",
    "xml2js": "^0.6.2"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "electron": "^29.1.0"
  },
  "build": {
    "appId": "com.yourcompany.pdfman",
    "productName": "PDFMan",
    "files": [
      "./main.js",
      "./package.json",
      "./server.js",
      "./data/**/*",
      "./node_modules/**/*",
      "./client/dist/**/*"
    ],
    "directories": {
      "output": "dist_electron"
    },
    "win": {
      "target": "nsis"
    },
    "mac": {
      "target": "dmg"
    },
    "linux": {
      "target": "AppImage"
    }
  }
}
